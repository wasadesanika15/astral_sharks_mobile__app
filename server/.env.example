PORT=3000
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=astral_sharks
DB_USER=postgres
DB_PASSWORD=password

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Production (Expo Web / hosted SPA): required comma-separated origins, e.g.:
# ALLOWED_ORIGINS=https://myapp.com,https://www.myapp.com,http://localhost:8081
